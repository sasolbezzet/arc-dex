# Maintenance

- Frontend source lives in `src/`; `dist/` is generated by `npm run build`.
- Keep wallet and transaction execution in the existing service modules. UI components must not read private keys.
- Validate changes with `npm run lint` and `npm run build`.
- Runtime secrets belong in `.env.local`; never commit environment files or temporary RPC test scripts.
